## 1. Confirm the routing contract

- [x] 1.1 Review this plan through the established orchestration plan-review checkpoint before implementation; record finding dispositions and any unavailable review evidence without treating missing code as a defect.
- [x] 1.2 Define versioned configuration/default/catalog files and example global/project documents; verify schema fixtures cover five interactions, native specialist overrides, atomic reasoning values, exact provider/model identifiers, and provisional four-tier mappings against current supported runtime evidence.

## 2. Implement shared resolution

- [x] 2.1 Implement configuration loading, validation, and field provenance in a Python standard-library helper; verify absent files, malformed JSON, unknown versions/keys, invalid types, and explicit run-only bypass behavior with isolated fixtures.
- [x] 2.2 Implement global/project/specialist/session precedence and reset inheritance; verify project interaction overrides global specialist settings, reasoning units do not mix modes, model-specific default ceilings and inherited explicit ceilings resolve correctly, and global files remain unchanged.
- [x] 2.3 Implement fixed and model-aware Adaptive resolution with tier/risk assessment inputs; verify Luna routine/high, complex/xhigh, exceptional/max versus xhigh ceilings, mechanical work, consequential risk floors, and fixed session overrides.
- [x] 2.4 Add runtime capability validation and structured JSON CLI output; verify unknown models, missing Adaptive profiles, unsupported effort, parent-attested native tool-description evidence versus guarded Claude launcher preflight sequencing, and unavailable metadata produce honest blocked/unverified states without fallback or model execution.
- [x] 2.5 Add immutable parent-owned snapshot files, mandatory snapshot-based dispatch resolution, and explicit reloads; verify identical inputs reproduce decisions and saved preference changes cannot silently alter an ongoing run.

## 3. Integrate orchestration and review

- [x] 3.1 Update coordinator guidance to classify interactions, invoke the resolver, validate native capabilities, and pass explicit worker arguments; verify source and installed walkthroughs preserve the parent settings, bounded context, role instructions, and concurrency limits.
- [x] 3.2 Update cross-review guidance to pass resolved model/effort into the existing launcher; verify mocked command capture uses saved routes/session overrides and existing direct-launcher defaults, subscription restrictions, skip criteria, read-only scope, and one-recheck limit remain intact.
- [x] 3.3 Extend parent-owned run-log guidance with snapshot provenance, assessment, ceiling effects, and requested versus observed settings; verify representative successful, capped, failed-dispatch, and reload entries against the log scenarios.

## 4. Provide local configuration access

- [x] 4.1 Implement atomic global/project saves with revision checks and exclusive per-destination coordination; verify missing-file creation, documented external-writer limits, owner-attributed lock recovery, stale revisions across two service instances, failed writes, lock contention, and prior-content preservation using temporary directories.
- [x] 4.2 Implement the loopback service with fixed startup-selected destinations and session authentication; verify unauthorized origin/host/token requests, arbitrary paths, oversized input, traversal, symlink/reparse escapes, and requests to execute agents are rejected.
- [x] 4.3 Expose load, validate/preview, and save endpoints using the shared resolver; verify API and CLI parity for the same saved and draft inputs, error responses, and absence of model/subscription calls.

## 5. Build the React editor

- [x] 5.1 Scaffold the React/TypeScript editor with verified compatible dependencies, a lockfile, focused test tooling, and versioned build metadata; verify a clean install and the editor's production build without changing normal Wrangler installation requirements.
- [x] 5.2 Implement global/project scope, five interaction rows, model selection, fixed/Adaptive controls, ceiling, and native specialist overrides; verify inherited field sources, reset behavior, project-disabled state, and read-only session-controlled parent settings through focused component tests.
- [x] 5.3 Implement advanced model-specific effort mappings and explainable draft previews; verify Luna ceiling examples, unknown-model handling, capability-unverified labels, and temporary session overrides that never enter saved preferences.
- [x] 5.4 Implement load/save/validation/conflict states with draft preservation and scope-switch protection for unsaved edits; verify saved confirmation follows successful persistence and errors remain associated with keyboard-accessible controls.
- [x] 5.5 Serve the built UI from source and installed layouts with compatibility checks; verify startup without assets, incompatible asset versions, session-token bootstrap/removal, and project selection produce the documented behavior.

## 6. Package and document

- [x] 6.1 Extend both Wrangler installers to package routing helpers/defaults and optional built editor assets as part of the existing single skill; verify fresh/repeated/upgrade installs in isolated roots preserve preference bytes, unrelated skills, and specialist migration behavior, and handle missing/stale owned assets explicitly.
- [x] 6.2 Document build/start commands, Python versus optional Node prerequisites, preference paths/precedence, isolated global directory override, documentation-only snapshot/lock/temp ignore patterns, reset/reload behavior, Adaptive limitations, direct-Claude compatibility, and rollback; verify commands and example configuration against the implemented helpers without modifying real global preferences.

## 7. Verify integrated behavior

- [x] 7.1 Run focused routing, local-service, installer, and existing Claude regression checks on Windows and a supported Unix environment; report exact results and platform skips without claiming unrun live account checks.
- [x] 7.2 Exercise the running editor in desktop and narrow viewports with the UI/UX reviewer, including keyboard editing, inherited project values, Luna previews, invalid drafts, save/reload, and conflicts; retain rendered evidence and recheck accepted fixes.
- [x] 7.3 Trace an isolated saved global profile plus project/session overrides through preview, coordinator dispatch preparation, Claude argument capture, and daily logging; verify identical effective requests, snapshot stability, fixed parent settings, and no automatic model substitution.
- [x] 7.4 Complete native correctness/spec review and the established Claude implementation checkpoint, reconcile findings, and run proportionate rechecks; record unavailable live checks or waivers explicitly and keep unsupported completion claims out of the final report.

## 8. Portable source-checkout launcher (approved follow-up)

- [x] 8.1 Add dependency-free root npm commands and a Node launcher for building, running the built editor, and running Python plus Vite with hot reload; verify portable argument handling, Python discovery, startup readiness, actionable prerequisite errors, and owned-process cleanup.
- [x] 8.2 Preserve loopback, token, Host and Origin boundaries through the development API proxy; verify real configuration/preview/save requests and rejected unauthorized requests using isolated preferences.
- [x] 8.3 Document initial build and source/installed runtime distinctions; run focused launcher checks and native review, recording actual platform coverage.

## 9. Scenario-specific specialist choices (approved follow-up)

- [x] 9.1 Filter override panels and preview choices by scenario, preserve editable configured exceptions and inherited overrides, and verify reset and scope behavior with component regressions and the editor build.

## 10. Stable Vite development port

- [x] 10.1 Use loopback port 42069 for Vite with strict port handling; retain the authenticated proxy and document startup behavior.

## 11. Bundled Astra Adaptive profile

- [x] 11.1 Add the default Astra four-tier profile and verify resolution, user ceilings, browser selection/save/reload and previews without requiring a manually created profile.

## 12. Claude CLI Adaptive profile compatibility

- [x] 12.1 Add review mappings for verified Claude CLI IDs, preserve exact aliases/preflight, and verify Opus save/reload/previews in browser automation.
